<?php

namespace PHPSTORM_META {
    override(\app(), map(['' => '@']));
    override(\App::make(), map(['' => '@']));
    override(\Illuminate\Support\Facades\App::make(), map(['' => '@']));

    override(
        \October\Rain\Extension\ExtendableTrait::asExtension(),
        map([
{% for beh in behaviors %}
            {{ beh }},
{% endfor %}
        ])
    );
}
