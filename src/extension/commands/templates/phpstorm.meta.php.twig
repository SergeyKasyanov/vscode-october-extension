<?php

namespace PHPSTORM_META {
    override(\app(), map(['' => '@']));

    override(
        \October\Rain\Extension\ExtendableTrait::asExtension(),
        map([
{% for beh in behaviors %}
            {{ beh }},
{% endfor %}
        ])
    );
}
