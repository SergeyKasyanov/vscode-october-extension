<?php

namespace {{ namespace }}\FormWidgets;

use Backend\Classes\FormWidgetBase;

class {{ widget_pascal }} extends FormWidgetBase
{
    protected $defaultAlias = '{{ namespace_snake }}_{{ widget_snake }}';

    public function init()
    {
        // $this->fillFromConfig([]);
    }

    public function render()
    {
        $this->prepareVars();

        return $this->makePartial('{{ widget_snake }}');
    }

    public function prepareVars()
    {
        $this->vars['name'] = $this->formField->getName();
        $this->vars['value'] = $this->getLoadValue();
        $this->vars['model'] = $this->model;
    }

    public function getSaveValue($value)
    {
        return $value;
    }
{% if addAssets %}

    public function loadAssets()
    {
        $this->addCss('css/{{ widget_snake }}.css', '{{ namespace_dot_capital }}');
        $this->addJs('js/{{ widget_snake }}.js', '{{ namespace_dot_capital }}');
    }
{% endif %}
}
